for Mac OSX and Linux.
Diretory iconv is using iconv utility.
You can change encoding of all files in a directory using iconv.

Usage:
$diconv arg1 arg2 arg3 arg4
arg1: directory name
arg2: file extension
arg3: from encoding
arg4: to encoding